@{
    ViewBag.PageTitle = "Våra Kurser";
}
@section styles {
    <link rel="stylesheet" href="~/css/course-page.css"/>
}


<div class="landing">
    <div class="content">
        <h1>Kurser vi erbjuder!</h1>
        <section>
        @foreach (var course in Model)
        {
            <div class="border-box">
                <a asp-action="details" asp-route-courseId=@course.CourseId>
                    <b>@course.CourseName</b><br>
                    Startdatum: @course.CourseStartDate<br>
                    Slutdatum: @course.CourseEndDate</a>
            </div>
        }
    </section>
    </div>
</div>
